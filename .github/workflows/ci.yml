name: Style and test

on:
  push:
    branches:
      - master
      - 'feature/**'
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 25 (Temurin) version
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'gradle'
          
      - name: Run Checkstyle (Main and Test)
        run: .\gradlew.bat --no-daemon checkstyleMain checkstyleTest

      - name: Run tests
        run: .\gradlew.bat --no-daemon test

      - name: Generate JaCoCo report
        run: .\gradlew.bat --no-daemon jacocoTestReport

      - name: Verify code coverage
        run: .\gradlew.bat --no-daemon jacocoTestCoverageVerification

      - name: Build project
        run: .\gradlew.bat --no-daemon build
